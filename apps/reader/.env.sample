# Copy this file to .env and fill in values for local development
# .env is git-ignored — never commit it

# Base path for the app (use / for Vercel, /monkai/ for GitHub Pages)
VITE_BASE_PATH=/

# URL for the book-data JSON (leave empty to use Vercel proxy rewrite from same origin)
VITE_BOOK_DATA_URL=
